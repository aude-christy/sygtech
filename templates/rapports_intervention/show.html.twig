{% extends 'base.html.twig' %}

{% block title %}RapportsIntervention{% endblock %}

{% block body %}
    <h1>RapportsIntervention</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rapports_intervention.id }}</td>
            </tr>
            <tr>
                <th>Libelle_rapport</th>
                <td>{{ rapports_intervention.libelleRapport }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ rapports_intervention.photo }}</td>
            </tr>
            <tr>
                <th>Piece_jointe</th>
                <td>{{ rapports_intervention.pieceJointe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rapports_intervention_index') }}">back to list</a>

    <a href="{{ path('rapports_intervention_edit', {'id': rapports_intervention.id}) }}">edit</a>

    {{ include('rapports_intervention/_delete_form.html.twig') }}
{% endblock %}
