{% extends 'base.html.twig' %}

{% block title %}Agences{% endblock %}

{% block body %}
    <h1>Agences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom_agence</th>
                <td>{{ agence.nomAgence }}</td>
            </tr>
            <tr>
                <th>Heure_ouverture</th>
                <td>{{ agence.heureOuverture }}</td>
            </tr>
            <tr>
                <th>Heure_fermeture</th>
                <td>{{ agence.heureFermeture }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ agence.longitude }}</td>
            </tr>
            <tr>
                <th>Lattitude</th>
                <td>{{ agence.lattitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('agences_index') }}">back to list</a>

    <a  href="{{ path('agences_edit', {'id': agence.id}) }}">edit</a>

    {{ include('agences/_delete_form.html.twig') }}
{% endblock %}
